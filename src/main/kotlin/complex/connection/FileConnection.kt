package complex.connection

class FileConnection(private val path: String) : Connection {

    override fun init() {
        // perform init
    }

    override fun connect() {
        // perform connect
    }
}